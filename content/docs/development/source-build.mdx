---
title: 从源码构建
description: 构建 ByteHop 服务端、CLI 和内嵌 Web UI，并运行基础检查。
---

这一节面向 ByteHop 的维护者。普通成员从[第一次使用](../quickstart)开始即可。

## 环境要求

- Go 1.26；
- Bun 1.4；
- 运行集成验收时还需要 Docker 与 Docker Compose。

## 构建

在源码根目录运行：

```bash
make build
./bin/bytehop version
```

`make build` 先构建 Web UI，再将静态资源嵌入同一个 Go 二进制。`bin/bytehop` 同时包含服务端和 CLI。

SQLite 使用 [`cznic/sqlite`](https://gitlab.com/cznic/sqlite) 的官方 module 路径 `modernc.org/sqlite`。这个驱动不依赖 CGo，因此发布脚本可以从同一个 Runner 交叉编译六个目标平台。

## 基础检查

```bash
make check
make docs-build
```

如果只改 Go 代码或 Web UI，也可以分别运行：

```bash
go test ./...
go vet ./...
(cd web && bun run typecheck && bun run build)
```

## 版本化分发

仓库的 `.github/workflows/release.yml` 在推送语义版本 tag（例如 `v0.1.0`）时自动完成：

1. 运行 Go 测试并构建内嵌 Web UI；
2. 交叉编译 Linux、macOS、Windows 的 amd64 / arm64 二进制；
3. 打包生产 Compose 示例并生成 `SHA256SUMS`；
4. 发布 GitHub Release；
5. 构建并推送 GHCR 的 linux/amd64 与 linux/arm64 镜像，同时生成 SBOM 和 provenance。

本地可以预演同一打包脚本：

```bash
make release-snapshot
```

## 发布第一个版本

先确认 `main` 已通过检查并推送，再创建带说明的 tag：

```bash
git switch main
git pull --ff-only
make check
git tag -a v0.1.0 -m "ByteHop v0.1.0"
git push origin main
git push origin v0.1.0
```

推送 tag 后，GitHub Actions 会同时生成 Release 附件和 GHCR 多架构镜像。可以用 GitHub CLI 查看进度与结果：

```bash
gh run list --workflow Release
gh release view v0.1.0
docker buildx imagetools inspect ghcr.io/rayui-lab/bytehop:0.1.0
```

仓库或镜像为私有时，需要先使用具备 `read:packages` 权限的 token 登录 GHCR，再执行 `docker pull`。正式版本 tag 不包含后缀；`v0.1.0-rc.1` 这类 tag 会作为预发布版本。

发布版本通过 Go linker flags 写入二进制，因此 `bytehop version`、`/healthz` 和管理界面显示同一个版本。

完成本地构建后，继续运行[本地验收环境](./local-stack)。
